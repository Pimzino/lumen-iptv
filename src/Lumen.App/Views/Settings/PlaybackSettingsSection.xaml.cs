using System.Windows.Controls;

namespace Lumen.App.Views.Settings;

/// <summary>Settings › Playback: stream preferences (HLS, hardware acceleration, user-agent).</summary>
public partial class PlaybackSettingsSection : UserControl
{
    public PlaybackSettingsSection()
    {
        InitializeComponent();
    }
}
